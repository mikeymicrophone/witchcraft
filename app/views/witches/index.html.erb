<h1>Listing witches</h1>

<table>
  <tr>
    <th>Witch name</th>
    <th>First name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @witches.each do |witch| %>
  <tr>
    <td><%= witch.witch_name %></td>
    <td><%= witch.first_name %></td>
    <td><%= link_to 'Show', witch %></td>
    <td><%= link_to 'Morph', edit_witch_path(witch) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Witch', new_witch_path %>
